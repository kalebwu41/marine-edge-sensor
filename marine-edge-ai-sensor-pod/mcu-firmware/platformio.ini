[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200

; If your board uses a different ESP32 variant, replace esp32dev with the correct board ID.
; Place additional library dependencies here if needed, for example:
; lib_deps = 
;   arduino-lmic
;   tensorflow/lite-micro@^2.11.0
